<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.SeviBus4" parent="Theme.AppCompat.DayNight.NoActionBar"></style>

    <!-- This theme is used by splashScreen.setSplashScreenTheme() to force a dark background when the user has forced dark mode. -->
    <style name="Theme.SeviBus4.Dark">
        <item name="android:windowSplashScreenBackground">#FF353238</item>
    </style>

    <style name="Theme.SeviBus4.Translucent">
        <item name="android:windowBackground">#00ffffff</item>
        <item name="android:colorBackgroundCacheHint">@null</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowAnimationStyle">@android:style/Animation</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:statusBarColor">#00ffffff</item>
    </style>
</resources>
